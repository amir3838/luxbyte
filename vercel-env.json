{
  "env": {
    "SUPABASE_URL": {
      "description": "Supabase Project URL",
      "required": true
    },
    "SUPABASE_SERVICE_ROLE_KEY": {
      "description": "Supabase Service Role Key (Private)",
      "required": true
    },
    "NEXT_PUBLIC_SUPABASE_URL": {
      "description": "Supabase Project URL (Public)",
      "required": true
    },
    "NEXT_PUBLIC_SUPABASE_ANON_KEY": {
      "description": "Supabase Anonymous Key (Public)",
      "required": true
    },
    "ADMIN_KEY": {
      "description": "Strong Admin Key for API Access",
      "required": true
    },
    "HMAC_SECRET": {
      "description": "HMAC Secret for Request Signing",
      "required": false
    },
    "ALLOWED_ORIGINS": {
      "description": "Comma-separated list of allowed origins",
      "required": true
    },
    "NEXT_PUBLIC_FIREBASE_API_KEY": {
      "description": "Firebase API Key (Public)",
      "required": true
    },
    "NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN": {
      "description": "Firebase Auth Domain (Public)",
      "required": true
    },
    "NEXT_PUBLIC_FIREBASE_PROJECT_ID": {
      "description": "Firebase Project ID (Public)",
      "required": true
    },
    "NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID": {
      "description": "Firebase Messaging Sender ID (Public)",
      "required": true
    },
    "NEXT_PUBLIC_FIREBASE_APP_ID": {
      "description": "Firebase App ID (Public)",
      "required": true
    },
    "NEXT_PUBLIC_FIREBASE_VAPID_KEY": {
      "description": "Firebase VAPID Key (Public)",
      "required": true
    },
    "FIREBASE_ADMIN_CREDENTIALS": {
      "description": "Firebase Admin Credentials (Private)",
      "required": false
    },
    "ALHARETH_API_KEY": {
      "description": "Alhareth Integration API Key",
      "required": false
    },
    "ALHARETH_API_URL": {
      "description": "Alhareth Integration API URL",
      "required": false
    }
  },
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}